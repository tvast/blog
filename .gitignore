# Local demo content: a mirror of personal notes, kept out of the published
# theme. The theme reads whatever collection the consuming site configures.
_knowledge/

# Build outputs - _site is local only, docs/ is committed for GitHub Pages
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
Gemfile.lock
vendor/
node_modules/
.DS_Store
.idea/
