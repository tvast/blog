# Local demo content: a mirror of personal notes, kept out of the published
# theme. The theme reads whatever collection the consuming site configures.
_knowledge/

# Build outputs. Rebuilt on demand for local previewing.

.jekyll-cache/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
Gemfile.lock
vendor/
node_modules/
.DS_Store
