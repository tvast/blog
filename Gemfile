# frozen_string_literal: true

source "https://rubygems.org"

# The theme itself, loaded from this directory so the demo site at the repo
# root exercises the same code the gem ships.
gemspec

gem "jekyll", "~> 4.4"
gem "jekyll-feed", "~> 0.17"
gem "jekyll-sitemap", "~> 1.4"
gem "webrick"
